apiVersion: v1
kind: Service
metadata:
  name: next-word-predictor-service
  namespace: default
  labels:
    app: next-word-predictor
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: next-word-predictor
